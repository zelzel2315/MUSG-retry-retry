<%= form_for @foundation do |f| %>

      <%= f.label :brand %><br>
      <%= f.text_field :brand %>

      <%= f.label :product %><br>
      <%= f.text_field :product %>

      <%= f.label :shade %><br>
      <%= f.text_field :shade %>

      <%= f.submit "Submit!" %>

<% end %>

